# Where the API lives.
#
# Normally leave this unset. The client then points at whatever machine served the page,
# on port 5211 — which is what makes the screens work on a phone or tablet on the same
# network without any per-device configuration.
#
# Set it only when the API is somewhere else entirely.
# VITE_API_URL=http://localhost:5211
